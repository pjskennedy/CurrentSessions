<div class="devise-view">
  <div class="row">
    <div class="well col-xs-12 col-sm-8 col-sm-offset-2 col-md-4 col-md-offset-4">
      
      <div class="page-header">
        <h2>Current Sessions Admin <small>Sign in</small></h2>
      </div>


      <hr/>

      <%= form_for(resource, :html => {:class => "form-horizontal", :role => "form"}, :as => resource_name, :url => session_path(resource_name)) do |f| %>

        <%= devise_error_messages! %>

        <div class="form-group">
          <label for="email" class="col-lg-3 control-label">Email</label>
          <div class="col-lg-9">
            <%= f.email_field :email, :class => "form-control", :id => "email", :placeholder => "bob@bob.bob" %>
          </div>
        </div>

        <div class="form-group">
          <label for="password" class="col-lg-3 control-label">Password</label>
          <div class="col-lg-9">
            <%= f.password_field :password, :class => "form-control", :id => "email", :placeholder => "Password" %>
          </div>
        </div>

        <% if devise_mapping.rememberable? -%>      
          <div class="form-group">
            <div class="col-lg-offset-3 col-lg-9">
              <div class="checkbox">
                <label>
                  <%= f.check_box :remember_me %> <%= f.label :remember_me %>
                </label>
              </div>
            </div>
          </div>
        <% end -%>


        <div class="form-group">
          <div class="col-lg-offset-2 col-lg-5">
            <%= f.submit "Sign in", :class => "btn btn-default btn-block btn-primary", :type => "submit" %>
          </div>
          <div class="col-lg-5">
            <%= link_to :new_admin_registration, :class => "btn btn-default btn-block" do %>
              To Register Page
            <% end %>
          </div>
        </div>

      <% end %>


    </div>
  </div>

</div>